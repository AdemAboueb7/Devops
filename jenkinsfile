pipeline {
    agent any

    stages {
        stage('Build & Package') {
            steps {
                bat 'mvn clean package -DskipTests'
            }
        }
    }

    post {
        success {
            echo '✅ Build réussi (tests ignorés).'
        }
        failure {
            echo '❌ Build cassé, va voir les logs.'
        }
    }
}